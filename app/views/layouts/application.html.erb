<!DOCTYPE html>
<html>
<head>
  <title>App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>
  <% flash.each do |type, message| %>
    <%= content_tag(:div, message, class: "flash #{type}") %>
  <% end %>

  <style>
    /* ... existing styles ... */
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 5px;
      background: #4CAF50;
    }
    .navbar-logo {
      display: flex;
      align-items: center;
    }
    .navbar-logo img {
      max-height: 50px; /* Adjust the size of logo */
    }
    .navbar-nav {
      display: flex;
      align-items: center;
      list-style: none;
      margin: 0; /* Remove margin to align items in line */
    }
    .nav-item {
      margin-left: 20px;
    }
    .nav-link, .cart-icon {
      color: white;
      text-decoration: none;
      display: flex;
      align-items: center;
    }
    .nav-link:hover {
      text-decoration: underline;
      height: 24;
    }
    .cart-icon img {
      max-height: 30px; /* Smaller size for the cart icon */
      margin-right: 10px;
    }
    .sign-out {
      display: flex;
      align-items: center;
      margin-left: 20px; /* Space between cart icon and sign-out */
    }
    /* Main container styles */
    .container {
      max-width: 1200px;
      margin: auto;
      padding: 0 15px;
    }
    .user-info {
      display: flex;
      align-items: center;
    }
    .user-info img {
      margin-right: 10px; /* Add space between the icons */
    }
    .navbar-nav ul {
      display: flex;
      padding-left: 0;
      margin: 0;
      list-style-type: none; /* Removes bullet points */
    }
    .navbar-nav li {
      margin-right: 20px; /* Space between nav items */
    }
  </style>
</head>
<body>
  <header class="navbar">
    <div class="navbar-logo">
      <%= link_to image_tag('logo.jpg', alt: 'Logo', height: '50'), root_path %>
    </div>
    
    <nav class="navbar-nav">
      <!-- Navigation links -->
      <ul>
        <li><a href="/">Home</a></li>
        <li><%= link_to "Listings", listings_path %></li>
        <li><%= link_to "Guide", guide_path %></li>
        <li><a href="/customization">Customization</a></li>
        <li><%= link_to "Gallery", gallery_path %></li>
        <li><a href="/about">About Us</a></li>
      </ul>
    </nav>
    <nav>  
      <% if user_signed_in? %>
        <div class="user-info">
          <!-- Cart icon -->
          <%= link_to image_tag('cart.jpg', alt: 'Cart', height: '24'), class: 'nav-link' %>
          <!-- Profile image -->
          <%= link_to image_tag('profile.jpg', alt: 'Profile', height: '24'), class: 'nav-link' %>
          <span>Hi, <%= current_user.email %></span>
          <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link', data: { turbo: false } %>
        </div>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link' %>
      <% end %>
    </nav>
  </header>

  <main role="main" class="container">
    <%= yield %>
  </main>
</body>
</html>
