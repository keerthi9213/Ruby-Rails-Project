<html>
<head>
  <style>
    a{
      color: white;
      text-decoration: none;
    }
    body{
      background: white;
      color: black;
    }
    .birdhouse-image {
      max-width: 100%;
      height: 200px; /* Set a fixed height for all images */
      object-fit: cover; /* Maintain aspect ratio and cover the container */
      border-bottom: 1px solid #eee;
      margin-bottom: 20px;
    }
    .review-button{
      background-color: #4CAF50; 
      color: white;
      padding: 10px 20px; 
      border: none; 
      border-radius: 5px; 
      cursor: pointer;
    }
    .container {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: space-between;
    }
  </style>
</head>
  <body>
   <h1>Purchase History</h1>
    <div class="container">
    <% @orders.each do |order| %>
      <div>
        <!--<%= image_tag "birdhouse4.jpg", class: 'birdhouse-image' %>-->
      <h3>Order ID: <%= order.id %></h3>
      <p>Total cost: <%= order.total_cost %></p>
      <p>Shipping Address: <%= order.shipping_address %></p>
      <p>Payment details: <%= order.card_type %> ending with <%= order.card_number.to_s[-4..-1] %></p>
      <%= link_to 'Add Review', '#', class: 'review-button' %>
        <!-- Other order details -->
      </div>
    <% end %>
    </div>
 

    <!--<div class="container">
      <% @orders.each do |order| %>
          <h3>Order ID: <%= order.id %></h3>
          
          <p>Total cost: <%= order.total_cost %></p>
          <p>Shipping Address: <%= order.shipping_address %></p>
          <p>Payment details: <%= order.card_type %> ending with <%= order.card_number.to_s[-4..-1] %></p>
          
          <%= link_to 'Add Review', '#', class: 'review-button' %>

      <% end %>-->

      <!--<div class="container">
      <% @orders.each do |order| %>
        <% order.line_items.each do |line_item| %>
        <div class="birdhouse-item">
      <%= image_tag("birdhouse#{line_item.birdhouse.id}.jpg", alt: 'Birdhouse image', class: 'birdhouse-image') %>
          <div class="birdhouse-item">
            <%= image_tag "birdhouse#{birdhouse.id}.jpg", class: 'birdhouse-image' %>
          <% end %>
            <div class="birdhouse-details">
              <p>Total cost: <%= order.total_cost %></p>
          <p>Shipping Address: <%= order.shipping_address %></p>
          <p>Payment details: <%= order.card_type %> ending with <%= order.card_number.to_s[-4..-1] %></p>
          
          <%= link_to 'Add Review', '#', class: 'review-button' %>
              
            </div>
          </div>
        <% end %>
      </div>-->
      <!--<div class="container">-->
  <!--<% @orders.each do |order| %>
    <% order.line_items.each do |line_item| %>
      <div class="birdhouse-item">
        <%= image_tag("birdhouse#{line_item.birdhouse.id}.jpg", alt: 'Birdhouse image', class: 'birdhouse-image') %>
        <div class="birdhouse-details">
           Details about the birdhouse 
        </div>
      </div>
    <% end %>
    <div class="order-details">
      <h3>Order ID: <%= order.id %></h3>
      <p>Total cost: <%= order.total_cost %></p>
      <p>Shipping Address: <%= order.shipping_address %></p>
      <p>Payment details: <%= order.card_type %> ending with <%= order.card_number.to_s[-4..-1] %></p>
      <%= link_to 'Add Review', '#', class: 'review-button' %>
    </div>
  <% end %>-->
  <!--<% @user.orders.each do |order| %>
    <% order.birdhouses.each do |birdhouse| %>
    <%= image_tag "birdhouses/#{birdhouse.id}.jpg", alt: "Birdhouse Image" %>
  <% end %>
    <div class="order-details">
      <h3>Order ID: <%= order.id %></h3>
      <p>Total cost: <%= order.total_cost %></p>
      <p>Shipping Address: <%= order.shipping_address %></p>
      <p>Payment details: <%= order.card_type %> ending with <%= order.card_number.to_s[-4..-1] %></p>
      <%= link_to 'Add Review', '#', class: 'review-button' %>
    </div>
  <% end %>-->
</div>

    </div>
    
  </body>
</html>
